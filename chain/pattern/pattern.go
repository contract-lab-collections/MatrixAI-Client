package pattern

// DOT is "." character
const DOT = "."

// Pallet
const (
	// OSS is a module about DeOSS
	OSS = "Oss"
	// SYSTEM is a module about the system
	SYSTEM = "System"
)

// Chain state
const (
	// ACCOUNT SYSTEM
	ACCOUNT = "Account"
)

// Transaction
const (
	// TX_OSS_REGISTER OSS
	TX_OSS_REGISTER = OSS + DOT + "authorize"
)
